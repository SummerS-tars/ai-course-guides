---
description: commit 后默认将改动同步到 master worktree（cherry-pick/ff-merge），并复制本地产物
alwaysApply: true
---

# 同步 master worktree

## 环境

| 项 | 路径 |
|----|------|
| Cursor worktree | `/home/thesumst/.cursor/worktrees/ai__SSH__wsl_/vkzo` |
| master worktree | `/home/thesumst/development/ai` |

同一 git 仓库的两个 worktree；在任一 wt 提交后，**默认**把该 commit 同步到 master wt。

## 默认约束（始终适用）

在 Cursor worktree 完成一项工作后，**除非用户明确说不要提交 / 不要同步**，Agent 必须：

1. **提交**：将本次改动的已跟踪文件 `git commit` 到当前分支。
2. **同步 commit**：在 master worktree 用 `git cherry-pick <hash>` 应用同一 commit；若历史已分叉且可安全快进，可用 ff-merge。**禁止 force**。
3. **复制非版本化产物**：对 git 忽略或未纳入版本库、但在本地仍需要的文件（如 `guides/*.pdf`、其他生成物），用 `cp` 到 master wt 的**等价路径**。**不要**复制 `node_modules/`、`.venv*`、`package-lock.json`（若已忽略）等依赖目录。
4. **不默认 push** remote，除非用户明确要求。
5. **收尾报告**：两 wt 的 HEAD hash 与 message、是否已复制产物、两 wt 工作区是否干净（gitignored 文件除外）。

## 默认流程（用户未说「不要同步」时）

1. 在本 wt 完成 `git commit`（排除 `guides/node_modules/`、`.cursor/skills/ai-guides-pdf/node_modules/`、`.cursor/skills/ai-guides-pdf/fonts/`、`.tools/`、未跟踪的 `*.pdf` 等本地工具目录；**勿提交** `node_modules`、`.venv`、字体 ttf 等）。
2. 在 master wt：`git cherry-pick <hash>`；若历史已分叉且可安全快进，可用 `ff-merge`，**禁止 force**。
3. 将需要的 gitignored / 未跟踪产物 **`cp`** 到 master wt 对应目录（见下节 PDF）。
4. **不默认 push remote**，除非用户明确要求。
5. 同步后简要报告：本 wt 与 master wt 的 HEAD commit hash 及 message，说明复制了哪些文件，并说明两 wt 工作区是否干净。

## PDF（guides）

- 课纲 PDF 位于 `guides/*.pdf`，由 git 忽略。
- 若 **`guides/AI课程-14周内容梳理.md`** 有更新：用 Puppeteer 重导 PDF（`npm install --prefix .cursor/skills/ai-guides-pdf && node .cursor/skills/ai-guides-pdf/scripts/export-pdf.mjs`），并 **`cp` 到 master wt 的 `guides/`**。
- 仅改单周学习指南（如 `AI-Week3-4-学习指南.md`）且课纲 md 未变：**不必**重导 PDF；若 vkzo 侧已有最新 PDF 且需与 master 对齐，仍可 **`cp`**。

## 禁止

- force push `master`
- 无用户明确要求时 amend / 改 git config

