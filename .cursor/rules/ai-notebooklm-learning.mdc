---
description: 人工智能(H)课程 NotebookLM 辅助学习工作流——提问设计、叙事整合、迭代规范
alwaysApply: false
globs: guides/**,**/1_人工智能H/**
---

# AI 课程 × NotebookLM 学习规则

> **版本**：v3（2026-06-10，单问单答 + 脚本落盘）  
> **标杆范例**：`guides/AI-Week1-2-学习指南.md`（整合层）；`notebooklm-raw/manifests/week3-4.json`（采集层）

## 适用范围

- 课程：CS30057h.01 人工智能（H），2025-2026 第 2 学期
- 资料根目录：`E:\★Document\1_Study\2_6_2026springTerm\1_人工智能H\`
- NotebookLM：AI Notebook（`505bdb1c`）
- 周次索引：`guides/AI课程-14周内容梳理.md`
- 历史 SOP：`~/service/openclaw/workspace/memory/notebooklm-study-guide-sop.md`（项目导向；本规则侧重按周学习指南）

## 角色分工

| 角色 | 职责 |
|------|------|
| **NotebookLM** | 阅读已导入资料，回答具体问题，标注来源 |
| **Agent** | 设计问题 → 调用 NotebookLM → **叙事串联** → 审核整合 → 吸收用户追问 → 输出/迭代文档 |
| **用户** | Review 文档，提出「为什么」「和 XX 什么关系」类追问，确认定稿 |

**原则**：Agent 以 NotebookLM 回答为**素材**，不是搬运工。必须补足 NotebookLM 不擅长的：**模块衔接、动机链、易混概念对比、用户追问的直观解释**。

## 五阶段工作流

```
Phase 0  资料盘点     本地目录 + NotebookLM source list 对齐
    ↓
Phase 1  NotebookLM 采集   L0→L4 分层提问（按模块分批，非一次问全周）
    ↓
Phase 2  初稿整合     术语表 + 知识地图 + 核心知识 + 易错点 + 串联
    ↓
Phase 3  叙事串联 ★    问题链、承接、小结——消除「子主题孤岛」
    ↓
Phase 4  用户迭代 ★    根据 Review 追问，补「追问」框、对比表、直观解释
    ↓
Phase 5  定稿         Checklist 全过，用户确认
```

**★ Week 1–2 经验**：Phase 1–2 产出的初稿信息够但**不好读**；Phase 3–4 是质量分水岭。未完成 Phase 3 不算可用学习指南。

### Phase 0：资料盘点

1. 列出本地 `1_人工智能H/` 目录（课程记录、课件、教材、Project）
2. `notebooklm source list` 与本地对齐，确认 Notebook 已收录
3. 标注缺失周次（如 7/9/11），后续整合时显式说明

### Phase 1：NotebookLM 采集（脚本化，原始内容落盘）

**原始存档目录**：`notebooklm-raw/`（与学习指南 `guides/` 分离）

```
notebooklm-raw/manifests/<module>.json   # 采集计划（prompt 分层）
notebooklm-raw/<module>/runs/<timestamp>/  # 每次运行的原始回答
  ├── run.meta.json / run.log
  ├── <batch-id>.prompt.txt
  ├── <batch-id>.answer.md
  └── <batch-id>.answer.json
```

**采集脚本**（推荐替代 Agent 内联 `notebooklm ask`）：

```bash
cd ~/development/ai
python scripts/nlm-collect.py notebooklm-raw/manifests/week3-4.json --dry-run
python scripts/nlm-collect.py notebooklm-raw/manifests/week3-4.json
python scripts/nlm-collect.py ... --only L0-positioning
python scripts/nlm-collect.py ... --resume notebooklm-raw/week3-4/runs/<ts>
```

脚本自动：`sync-auth`、代理、`notebooklm use/clear/ask --json`、落盘、写 `run.meta.json`。

**Agent Phase 2** 从 `notebooklm-raw/.../runs/<ts>/*.answer.md` 读素材整合，**无需把原始回答塞进对话上下文**。

详见 `notebooklm-raw/README.md`。

**手工采集时的技术前置**（仅调试或补跑单条时使用）：

```bash
cd ~/service/openclaw/workspace/skills/notebooklm-integration
python3 scripts/sync-auth.py
export HTTPS_PROXY=http://127.0.0.1:7897 HTTP_PROXY=http://127.0.0.1:7897
source .venv/bin/activate
notebooklm use 505bdb1c
```

认证失效：Windows `fix_login_edge.py` → WSL `sync-auth.py --force`。

**NotebookLM 提问习惯（v3，用户验证有效）**：

```
Step 1  L0 单问：大方向下有哪些子主题？（可 Agent 预拆分 + NotebookLM 校验）
Step 2  每个子主题单独一次提问（manifest 中一个 batch = 一个 chat = clear_conversation: true）
        → prompt 内标注重要程度：核心 / 重要 / 了解即可，控制回答深度
Step 3  复杂子主题再拆（如 BP 回归、BP 分类、隐层 δ 各一问；不要 L1+L2 合并）
Step 4  学习后不懂的 → 追加 supplement batch（直观解释 / 数值样例 / 对比表）
```

**禁止**：一个 prompt 里塞多个模块（如「讲清 Week3 全部 BP + Week4 全部 CNN」）。

**manifest 字段**：`priority`（critical/important/normal）、`layer`、`clear_conversation: true`（默认全开新对话）。

**提问批次模板**（以一周或两周为单位，约 15–25 个 batch）：

| 类型 | 示例 batch id |
|------|----------------|
| L0 定位/子主题地图 | `L0-positioning`, `L0-subtopic-map` |
| 单知识点 L1/L2 | `w3-bp-regression`, `w4-conv-params` |
| L3 数值/易错 | `w3-bp-numeric`, `w34-mistakes` |
| L4 串联/PJ映射 | `w34-pj1-map`, `w34-bridge` |
| 用户追问补充 | `supplement-<topic>`（Step 4 追加） |

**资料引用优先级**（Prompt 与整合时均遵守）：

1. 课程记录 `week{N}-周五-AI.*`
2. 课件 `0{N}*.pdf`
3. 教材（AIMA / 花书 / Luger）
4. 其他（Project 文档、已生成指南）

**L0–L4 层次**：

| 层次 | 目标 |
|------|------|
| L0 | 在课程中的位置、核心问题、子主题清单 |
| L1 | 大白话 + 生活类比 |
| L2 | 公式/定义，每个符号注明含义 |
| L3 | 数值例子、推导动机、易错点对比表 |
| L4 | 与相邻周、Project 的串联 |

**Prompt 必含**：中文、点名周次+课件编号、要求标注来源、要求类比/数值例子（L1/L3）、当前层次标注。

**Prompt 补充**（v2 新增，效果好）：

- L0：「请说明各子主题之间的逻辑递进关系，哪些问题是上一节自然引出的？」
- L3：「请列出本模块最易混淆的 3–5 组概念，每组用对比表说明：为什么易混、正确理解、记忆技巧」
- 概念桥接：「请对比 [概念 A] 与 [概念 B] 的异同，说明各自在什么场景下出现」

### Phase 2：初稿整合

按下面文档结构组装 NotebookLM 产出，**去重、统一符号、修正明显错误**（如 NotebookLM 偶发乱码/截断）。

### Phase 3：叙事串联（关键）

**问题**：初稿子主题独立，读者不知道「为什么要学下一节」。

**解法**：三层叙事结构

**（1）章级叙事线**——大节开头用问题链总览：

```markdown
> **本节叙事线**：
> A. [能做什么？] → B. [硬边界在哪？] → C. [替代出路？] → D. [最终方案] → E. [新问题]
```

**（2）节级「要回答什么」**——每个子节开头一句：

```markdown
> **本节要回答**：[一个具体问题]
```

**（3）节间衔接**——每节末尾 + 下节开头：

```markdown
**A 节小结**（3 条以内）→ 抛出追问「是否存在…？」

---

#### B. [下节标题]
> **承接 A 节**：[为什么必须学 B；B 要验证/解决 A 留下的什么问题]
```

**原则**：读者任何时候都能回答「我刚学完的这节，是为了解决什么问题；下一节又是为什么」。

### Phase 4：用户迭代

用户 Review 中的「为什么」「有什么区别」「什么场景下」是**高质量补缺信号**。

**处理方式**：

1. 在回复中直观解答
2. **回写进指南**，使用固定格式，便于后续读者：

| 块类型 | 何时用 | 格式 |
|--------|--------|------|
| **追问** | 读者会自然产生的「为什么」 | `> **追问：…**` + 解答 |
| **直观理解** | 公式/矩阵运算不直观 | `> **直观理解…**` + 场景化解释 |
| **对比表** | 易混概念对（b vs t、MP vs 感知机、手工 φ vs MLP） | Markdown 表格 |
| **易错点展开** | §3 表格放不下、需场景说明 | `#### 易错点展开说明` |

**典型需主动补强的桥接问题**（即使用户没问，也应检查）：

- 手工设计 $\phi$ 引入非线性 vs MLP 权重是线性的，非线性从哪来？→ **答：激活函数 $f$；无 $f$ 则多层 $W$ 仍等价单层**
- 正规方程 vs 梯度下降：何时用哪种？
- 参数对称性：不只是「有多个解」，而是「谷底之间有山脊」→ 非凸

### Phase 5：定稿

跑完 Checklist，用户确认后标记为定稿。已定稿范例：`guides/AI-Week1-2-学习指南.md`。

## 文档结构（按周/按模块）

```markdown
# [周次] 学习指南

## 0. 术语表（大白话 + 类比，🔗 标记）

## 1. 知识地图（L0）

## 2. 核心知识
> **本节叙事线**（章级问题链）

### 2.x [子主题]
> **本节要回答**：…
（L1→L2→L3 整合；关键处用「追问」「直观理解」块）
**x 节小结** → 引出下一节

## 3. 重难点与易错点（表格 + 必要时「易错点展开」）

## 4. 知识串联（L4）

## 5. 资料索引 + 学习建议
```

## 质量 Checklist

### 内容完整性

- [ ] 每个公式附符号表
- [ ] 术语首次出现 🔗 指向术语表
- [ ] 难点有类比或数值例子
- [ ] 关键结论可追溯到来源（FiCS 记录 / 课件编号）
- [ ] 无 NotebookLM 跨轮重复（已去重）
- [ ] 缺失周次已标注

### 叙事与可读性（v2 新增，**必查**）

- [ ] 核心知识大节有「叙事线」总览
- [ ] 每个子节有「本节要回答」
- [ ] 相邻子节有「小结 → 承接」；读者能说出「为什么要学下一节」
- [ ] 易混概念有对比表（≥2 组）
- [ ] 关键公式/算法有「直观理解」或「追问」块（≥1 处）
- [ ] 与前后周/Project 的桥接在 L4 或节间小结中体现

### 技术验证

- [ ] NotebookLM `auth check --test` 通过（需代理）
- [ ] 本地资料与 Notebook source 对齐
- [ ] Phase 1 原始回答已落盘至 `notebooklm-raw/<module>/runs/<ts>/`（`run.meta.json` 状态 completed 或目标 batch 已 ok）

## 输出位置

- 按周指南：`guides/AI-Week{N}-学习指南.md` 或 `guides/AI-Week{N-M}-学习指南.md`
- 按 Project：`5_Project/` 或 `guides/`

## 禁止事项

- 一次问整周/整章所有内容
- 泛泛说「相关课件」不点名编号
- 只粘贴 NotebookLM 输出不做叙事串联
- Agent 编造内容不标注来源
- 未完成 Phase 3 即声称「学习指南已完成」

## 经验摘要（持续更新）

| 经验 | 来源 |
|------|------|
| 点名周次+课件比泛泛提问效果好 10 倍 | PJ1 SOP、Week 1–2 |
| 分模块提问信息密度远高于一次问全 | PJ1 SOP、Week 1–2 |
| 类比+数值例子是降低认知负荷最有效手段 | PJ1 SOP、Week 1–2 |
| **叙事串联是「能读」与「信息堆砌」的分水岭** | Week 1–2 迭代 |
| **用户追问是文档补强的最佳清单** | Week 1–2 迭代（Δw·xᵢ、阶跃函数、正规方程、对称性、φ vs MLP） |
| WSL 访问 NotebookLM 必须走 `127.0.0.1:7897` 代理 | 环境验证 |
| `auth check` 浅层通过 ≠ 可用；须 `--test` 验证 token | 环境验证 |
